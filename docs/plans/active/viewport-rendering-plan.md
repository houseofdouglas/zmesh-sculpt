# Execution Plan: viewport-rendering

Started: 2026-07-23
Status: COMPLETE (all 9 tasks done 2026-07-23; acceptance PARTIAL — ≥30fps@Max not met, see Task 09's Decisions & Notes)

Spec: docs/specs/viewport-rendering.md
Tasks: docs/tasks/viewport-rendering-tasks.md

## Progress
- [x] 01 — Install three + renderer init, capability detection, dev harness
- [x] 02 — Pure math helpers (spherical-camera, grab-projection, dirty-range)
- [x] 03 — Scene: lights, clay material, static default camera, resize
- [x] 04 — Mesh-sync: SculptMesh → geometry, partial upload, topology rebuild
- [x] 05 — Camera controller: orbit / pan / zoom + frameModel
- [x] 06 — Picking: raycast → SurfaceHit (point, normal, grab worldDelta)
- [x] 07 — Pointer router: classifyMode + camera/sculpt event routing
- [x] 08 — Brush cursor ring + mirror-plane indicator + setBrushDisplay
- [x] 09 — Viewport facade: attachEngine, lifecycle/dispose, onFrameStats, perf verification (see note: ≥30fps@Max not met, see below)

## Decisions & Notes
- Task 01: `three@0.185.1` ships **no bundled types** (`types`/`typesVersions` both absent from its package.json) — installed `@types/three` (matched exact version) and `@webgpu/types` (for `navigator.gpu` capability typing) separately. `three/webgpu` resolves correctly under this project's `moduleResolution: "bundler"`; verified with a throwaway probe file — testing module resolution via a direct `tsc <file>` CLI arg is misleading (it ignores `tsconfig.json` entirely and defaults to classic resolution), the probe had to live under `src/` to pick up the real project config.
- Task 01: `renderer.backend` is typed as the abstract base `Backend`, not the concrete `WebGPUBackend`/`WebGLBackend` — `isWebGPUBackend` only exists on the subclass, so backend detection uses an `in` check (`'isWebGPUBackend' in renderer.backend`) as both the runtime check and the type guard, rather than an unsafe cast.
- Task 01: `.viewport`'s CSS was `display:flex; align-items:center` (from Task 01 of sculpt-engine-core, meant only for centering placeholder text) — that container mode doesn't reliably stretch a percentage-sized canvas child. Switched to `position:relative` on the container with the canvas `position:absolute; inset:0`, which is robust regardless of the container's own layout mode. Verified visually: canvas fills edge-to-edge at both desktop and tablet viewport presets.
- Task 01: verified against a **real WebGPU backend** in this environment (`backend: webgpu` in the running app) — the WebGL2 fallback path and the "neither backend available" failure path are implemented and code-reviewed but not exercised against an actual non-WebGPU browser in this session (nothing in this environment lacks WebGPU to test against).
- Task 01: live window-resize handling (a resize listener updating `renderer.setSize` after mount) is explicitly Task 03's scope, not this one — Task 01's `resize()` call only runs once at `init()` to size correctly for whatever the container's dimensions are at mount time. Verified only that initial-mount sizing is correct across viewport presets, not live resize-while-mounted.
- First spec with interactive browser verification — most tasks are browser-verified via `npm run dev` (launch config `zmesh-dev`, port 5173), not just Vitest. Only Task 02 (pure math) and Task 07's `classifyMode` are Node-tested.
- A throwaway dev harness in `src/App.tsx` (news up an engine + viewport, mounts it) is introduced in Task 01 and fleshed out per task. The UI spec replaces it — its eventual deletion is expected, not a regression.
- Milestone shape: real sphere on screen by Task 04; full pointer→engine→mesh→screen sculpt loop closed by Task 07; end-to-end ≥60fps perf criterion (the one left PARTIAL by the engine's headless benchmark) validated in Task 09.
- Spec Open Questions planned toward their leaning answers: in-house camera math (Task 05), oriented-ring cursor (Task 08), brush radius via `BrushDisplayConfig` not an engine getter (Task 08). Revisit at implementation if any proves wrong.
- Task 07 (pointer router) is the fiddly one (trackpad gesture detection: pinch arrives as wheel+ctrlKey, etc.); kept as one task but a candidate to split into mouse vs trackpad if it gets unwieldy.
- Task 02: added `math/vec3.ts` (add/subtract/scale/dot/cross/length/normalize over a `readonly [number,number,number]` tuple), not listed in the spec's own Architecture table — all three named modules need the same handful of vector operations, so a small shared helper avoided hand-rolling cross/dot/scale three times. Framework-free like the rest of `math/` (no `THREE.Vector3`), so it stays Node-testable.
- Task 02: `framingDistance` treats the bounds diagonal as a worst-case bounding-sphere diameter (`d = diagonal / (2*tan(fovY/2))`) and applies a small feel-tuned margin (1.15×) above the tight fit so the framed model doesn't touch the exact view edge — margin only increases the returned distance, so it can't violate the acceptance criterion's "projected extent ≤ view height" (it only ever loosens that inequality further).
- Task 02: `projectScreenDeltaToWorld`'s depth is measured **along `cameraForward`** (a projection of `grabPoint - cameraPosition` onto the forward axis), not the straight-line distance to `grabPoint` — the correct quantity for a perspective FOV, and consistent with `framingDistance`'s own distance-along-view-direction convention. Screen Y is negated when mapping to the world "up" axis, since screen-space Y conventionally grows downward.
- Task 02: one test (`yields zero delta for zero screen motion`) initially failed on a `-0` vs `0` mismatch from `toEqual` on the returned tuple — `-screenDy` with `screenDy=0` produces `-0` in the up-component multiply, which is numerically identical to `0` (`-0 === 0`) but distinguished by `toEqual`'s `Object.is` semantics. Not a real bug; fixed by asserting each component with `toBeCloseTo` instead of a whole-tuple `toEqual`.
- Task 03: the static default camera's distance is derived from `framingDistance` (Task 02's math) applied to the placeholder sphere's own AABB-diagonal — not a hand-picked constant — so Task 05's `frameModel()` will reuse the exact same formula against the real mesh's bounds later, rather than the static setup and the real framing logic drifting to different tuning.
- Task 03 — **real bug caught interactively, not by Vitest**: on load, the browser console showed a cascade of `THREE.WebGPURenderer` uncaptured `GPUValidationError`s (zero-size depth-buffer/swapchain textures). Root cause: React 19 StrictMode's dev-mode mount → cleanup → mount double-invoke calls `viewport.dispose()` on the first `Viewport` instance while its async `init()` is still awaiting `initRenderer()`; `init()` had no guard against this, so it went on to build a renderer/scene/resize-observer/render-loop against a canvas already removed from the DOM (zero size) once the awaited promise resolved. Fixed by checking `this.disposed` immediately after the `await` and disposing whatever `initRenderer` produced instead of wiring it in, returning `{ ok:false, reason: 'viewport was disposed before init() completed' }`. Verified by reloading in a **fresh** browser tab (a reused tab's console buffer had stale pre-fix errors still visible, which briefly looked like the fix hadn't worked) — zero console errors after the fix.
- Task 03: resize is handled via a `ResizeObserver` on the container (not a `window.resize` listener), so container-driven layout changes (not just the browser window) are caught; each resize call also refreshes `setPixelRatio` in case DPR changed (e.g. dragging the window across displays), alongside `setSize` and `camera.aspect`/`updateProjectionMatrix` — verified visually at both mobile (375×812) and desktop presets: the sphere stays a circle, never an oval, at either aspect ratio.
- Task 04: `mesh-sync.ts` wraps the engine's `positions`/`normals`/`indices` typed arrays **directly** as `THREE.BufferAttribute`s (not copies) — FR-5's requirement, and what makes a partial sync cheap: the CPU-side geometry data is already correct the instant a stamp mutates it in place, so `sync()` only ever needs to tell the GPU which byte range to re-upload, never touch the JS-side values itself.
- Task 04: distinguishing "same topology, partial update" from "different topology, rebuild" is done by comparing `sculptMesh.positions !== currentMesh.positions` (object identity of the typed array itself), not by inspecting the `DirtyRegion`'s numeric range — a remesh always produces a brand-new `SculptMesh` with brand-new typed arrays even if it happened to land on the same vertex count, so identity is the one signal that's correct by construction, with no ambiguous edge case to reason about (e.g. "does a coincidentally-full-range stroke count as a rebuild?").
- Task 04: deliberately does **not** recompute `geometry.boundingSphere` on every partial sync (only on rebuild) — it's an O(vertexCount) scan, exactly the kind of per-stamp cost the engine spec's own Q-01 finding flagged as the scaling bottleneck. Accepted, flagged risk: a long session that pushes vertices well outside the last-computed bounding sphere (repeated Inflate, say) could in principle cause Task 06's raycasts to miss near the edge, since Three.js's raycaster uses the bounding sphere as an early-out. Revisit if that shows up once picking exists.
- Task 04: added a basic `Viewport.attachEngine(engine)` ahead of its official Task 09 slot (subscribes `onChange`, replaces the Task 03 placeholder with the engine's real mesh) — the task description itself said to "update the harness to construct a SculptEngine," which needs *some* way to get the resulting mesh into the scene, and building the simple version now / hardening it later (Task 09 adds unsubscribing a *previous* engine on re-attach) mirrors the established stub-then-complete pattern from the engine spec (e.g. Task 15's `setDetail` stub, completed in Task 16).
- Task 04 — **two real bugs caught interactively, not by Vitest, both producing the same symptom** (a cascade of `THREE.WebGPURenderer` zero-size-texture `GPUValidationError`s on load, self-recovering after a frame or two): (1) `dispose()` could remove the canvas from the DOM while `init()`'s `initRenderer()` call was still in flight — fixed by tracking the pending promise and deferring canvas removal until it settles. (2) The **actual** root cause, found by temporarily disabling StrictMode entirely and seeing the *same* errors with only one mount: the container's `clientWidth`/`clientHeight` genuinely read `0x0` on the first one or two `resize()` calls in this environment (both the manual call in `init()` and `ResizeObserver`'s own first callback), before settling to the real size — nothing to do with React at all. Fixed by skipping `resize()`'s work (and skipping `render()` in the loop) whenever the container reports a zero dimension, letting the later, real resize correct it. (1) is still worth keeping — it's a genuine, if rarer, race — but was not this bug's cause; confirmed by reproducing with StrictMode fully removed and the fix still being needed. Verified clean (zero console errors) across multiple fresh-tab reloads after the real fix, both with and without StrictMode.
- Task 05: `scene.ts` (Task 03) now returns `initialCameraDistanceMm` (the exact distance it used to frame the placeholder) alongside the `ViewportScene` it already returned — so `Viewport` can construct the `CameraController` with `{ target: [0,0,0] (default), yaw: 0 (default), pitch: 0 (default), radius: initialCameraDistanceMm }` and reproduce the *identical* camera transform scene.ts already set, with zero visual jump, rather than duplicating the placeholder-diameter constant and framing math a second time.
- Task 05: `pan`'s sign convention was chosen deliberately — the target (and thus camera) moves by the *negative* of the raw `projectScreenDeltaToWorld` result, so on-screen content tracks the cursor direction (the same "grab and drag" feel as the engine's Grab brush), rather than running away from it. `CameraController` also derives the camera's true orthonormal up vector itself (`trueUp`, via the standard look-at basis construction) for this call, rather than reading `camera.up` — Three.js's `.up` stays whatever fixed world-up hint was last assigned (it's an input to `lookAt`, not an output), so it doesn't reflect the camera's actual current orientation once pitch is nonzero.
- Task 05: added a basic `Viewport.attachEngine` extension — calling `cameraController.frame(engine.getMesh().bounds)` once, right after wiring up mesh-sync — satisfying FR-10's "frame on mount / on a new mesh" for the one call site that currently exists. **Left open**: reframing on a *subsequent* `loadMesh`/`newFromPrimitive` on an already-attached engine (as opposed to the initial attach) needs a signal distinguishing that from a remesh that the current `onChange`-only wiring can't yet provide — both produce an identical full-mesh `DirtyRegion` from the viewport's point of view. Flagged for whoever completes the full `attachEngine` contract (Task 09) or a future engine-layer signal.
- Task 05: `orbit`/`pan`/`zoom` are exposed as temporary, non-spec public methods on `Viewport` (only `frameModel()` is in the spec's own `Viewport` interface) purely so this task's own text — "exercised from the harness with temporary buttons or keys" — could be satisfied without the harness reaching past the `Viewport` facade into `CameraController` directly. Likely superseded once Task 07's pointer router calls `CameraController` itself.
- Task 05: verified interactively — orbit (yaw and pitch separately; pitch was the more legible check since the scripted Draw bump sits exactly on the yaw rotation axis at the sphere's pole, so pure yaw doesn't visibly move it, while pitch swings it from the silhouette edge to dead-center as expected), pan (content tracked the drag direction), zoom (visible scale change), `frameModel()` (re-centered and re-fit after pan+zoom drift, without resetting the orbit angle), and confirmed a remesh does *not* reframe the camera (identical view before/after `setDetail`).
- Task 06: Three.js's own `Mesh.raycast()` already computes an **interpolated** (barycentric-weighted, via the geometry's `normal` attribute) vertex normal at the hit point — not the flat face normal — and even flips it to face the ray; found by reading `three/src/objects/Mesh.js` directly rather than assuming from the type declarations alone (`Intersection.normal` is typed as optional with no doc comment explaining what populates it). It isn't guaranteed unit-length after interpolation, though, so `pickSurfaceHit` re-normalizes it explicitly to satisfy the spec's "unit normal" wording rather than assuming the library's output already is.
- Task 06: `computeGrabWorldDelta` derives the camera's forward direction via `camera.getWorldDirection()` and its **true current** up vector via `camera.up.clone().applyQuaternion(camera.quaternion)` — not the raw `camera.up` property, which stays whatever fixed world-up hint was last assigned (an input `lookAt` consumes, not a live output reflecting orientation) — the same distinction `CameraController.trueUp` already had to make in Task 05, here derived from the Three.js camera object directly instead of from spherical state, since `picking.ts` doesn't (and shouldn't) depend on `CameraController` — it works off whatever camera is active, regardless of what's driving it.
- Task 06: `pick`/`grabDelta` are temporary, non-spec `Viewport` methods (matching Task 05's `orbit`/`pan`/`zoom` precedent) purely for this task's own harness-based verification; Task 07's pointer router will most likely call `picking.ts` directly.
- Task 06: verified interactively via click-to-pick (logged to console and a small on-screen readout, updated by direct DOM mutation rather than React state to avoid a re-render per click): a click on the sphere returned `point≈[0.8, 1.4, 24.9]` and `normal≈[0.03, 0.06, 1.00]` (unit-length, pointing at the camera as expected for a near-center click on the default front-facing view); a click on empty background space returned no hit; and a "Test Grab delta" button computed `computeGrabWorldDelta` at the last picked point for a 50px rightward screen delta, yielding `[5.307, 0, 0]` — purely along the camera's actual right axis with zero Y/Z, confirming the real-camera data extraction (not just the already-unit-tested pure math from Task 02) is correct.
- Task 07: desktop browsers report a trackpad's 2-finger drag, shift+2-finger drag, and pinch **all as `wheel` events**, not pointer events — there is no "how many fingers" signal at the pointer-event level on desktop. Only genuine single-pointer drags (1-finger trackpad, or any real mouse button) arrive as `pointerdown`/`pointermove`/`pointerup`, which is why `pointer-router.ts` ended up as two genuinely separate mechanisms (pointer-event gesture routing vs. wheel-event gesture routing) rather than one unified input pipeline.
- Task 07 — **a real, deliberate deviation from FR-14's literal mouse row, chosen and documented rather than silently picked**: an unmodified `wheel` event defaults to **orbit**, not the Mouse row's "scroll = zoom." JS cannot distinguish a trackpad's unmodified 2-finger drag from a physical mouse's scroll wheel at the `wheel`-event level — both arrive identically — so *some* choice had to be made for the unmodified case, and this app is explicitly trackpad-first (per project memory: Peter's primary input device), making FR-15's "2-finger drag always orbits" the more load-bearing requirement to preserve exactly. A mouse's zoom is still fully reachable via ctrl+scroll (the same pinch pathway). `classifyWheelGesture`'s doc comment carries this reasoning; revisit if it ends up feeling wrong for mouse users once actually used.
- Task 07: `pan`'s pointer-drag sign convention matches `CameraController.pan`'s own "content tracks the cursor" feel (Task 05) — dx/dy are passed straight through, since `CameraController.pan` already negates internally. Orbit's pitch is inverted (`-dy`) so dragging up tilts the view up (a common trackball-style convention); yaw is not inverted. Both are feel-tunable constants (`DRAG_ORBIT_RADIANS_PER_PIXEL`, `WHEEL_ORBIT_RADIANS_PER_UNIT`, `WHEEL_ZOOM_SENSITIVITY`), same class as other tuned-by-feel values elsewhere in this codebase.
- Task 07 — **two things caught only through interactive/synthetic-event testing, neither a real application bug**: (1) `canvas.setPointerCapture(pointerId)` throws `NotFoundError` for a *synthetic*, untrusted `PointerEvent` (no genuinely-active OS pointer session backs the given id) — real user-initiated pointerdowns never hit this, but the call is wrapped in a silent try/catch anyway now, since capture is a nice-to-have (smoother dragging past the canvas edge) and a rare pointerId edge case failing it shouldn't abort the rest of gesture handling. (2) An initial pinch-zoom test with an unrealistically large single-event `deltaY=-300` appeared to put the camera "inside" the sphere (a flat, edge-to-edge gradient, no visible silhouette) — turned out to be mathematically correct, not a bug: at the resulting close distance, the sphere's angular size exceeds the camera's FOV, so its silhouette edge simply falls outside the visible frustum. Confirmed by re-testing with a realistic delta (±20, typical of one real scroll/pinch tick), which zoomed sensibly with the silhouette intact.
- Task 07: also independently confirmed (relevant to earlier tasks' interactive verification) that this Browser pane's console-reading tool consistently reports every real log line **twice** — verified by attaching a counting listener and dispatching a single event, which fired the handler exactly once. This is a tooling/display artifact of this environment, not a double-execution bug in the app; earlier "everything appears twice" observations in this plan's notes share the same explanation and were not evidence of anything wrong.
- Task 07: verified interactively — drag-on-model sculpts (a continuous raised ridge along the drag path) and drag-on-empty-space orbits; a *synthetic* right-drag (button=2) starting at the canvas center (over the model) orbited without sculpting, confirming FR-15; Shift+right-drag panned (confirmed by the sphere's screen position visibly shifting); ctrl+wheel zoomed (silhouette same, size changed); an unmodified wheel event orbited (subtle shading shift, size unchanged); a drag that started centered on the model and moved through clearly-empty-space corners stayed in sculpt mode the whole time (a bump appeared at the start point; no camera movement occurred despite passing through off-mesh positions); and an Alt-held drag on the model produced a clear indent rather than a bump, confirming brush invert.
- Task 08: the ring is built once at a **unit** radius (`RingGeometry(0.85, 1, 48)`) and resized purely via `ring.scale.setScalar(cursorRadiusMm)` rather than rebuilt — keeps `setBrushDisplay` cheap enough to call on every slider tick rather than every geometry-rebuild. Both the ring and the mirror plane render with `depthTest: false` + a high `renderOrder` rather than relying solely on a small along-normal offset to dodge z-fighting with the surface underneath.
- Task 08: rather than adding a second, separate hover-tracking listener, `pointer-router.ts`'s existing `pointermove` handler (Task 07) was extended to *also* drive the cursor ring — reusing whatever hit it already raycasts for passive hover or an active sculpt drag (no extra raycast), and explicitly hiding the ring (`updateCursor?.(null)`) whenever the active gesture is camera-mode (pointer-driven orbit/pan, or *any* wheel-driven gesture) — satisfying "hides ... when a camera gesture is active" directly from state `pointer-router.ts` already tracks, rather than `cursor.ts`/`viewport.ts` needing to infer gesture state some other way.
- Task 08: the mirror plane is edge-on (invisible) from the viewport's default front-on camera angle, since it's the x=0 plane spanning Y/Z and the default view looks straight down Z — had to orbit the camera before its on/off toggle was visually checkable. Not a bug, just a reminder for anyone re-verifying this later: check from an angled view, not the default framing.
- Task 08: verified interactively — a synthetic passive hover (pointermove, no buttons) over the model showed a correctly oriented ring at the hit point; hovering to an empty corner hid it; three "cursor radius +2mm" clicks followed by re-hovering showed a visibly larger ring at the same position; orbiting to an angled view and toggling symmetry showed/hid the mirror plane (a translucent blue-tinted plane bisecting the sphere at x=0) while leaving the cursor ring unaffected; and a synthetic right-drag (camera gesture) starting directly over the model hid the ring for the gesture's duration even though the pointer never left the mesh, confirming the "hides during a camera gesture" behavior specifically (not just the simpler off-mesh case).
- Task 09: `attachEngine` now fully unwinds a prior attachment before wiring the new one — unsubscribes the old `onChange`, removes and disposes the old `MeshSync` — rather than the Task 04 basic version's "leaks on re-attach" caveat. `onFrameStats` computes fps from the real render loop (frame count / elapsed time over ~1-second windows, matching the engine's own `onRemeshProgress` precedent for small additive extensions to the spec's drafted interfaces) rather than any synthetic per-frame estimate.
- Task 09 — **found and fixed a severe, previously-undiscovered bug: manifold-3d's WASM file failed to load from inside the remesh Web Worker, in both the Vite dev server and a production build, meaning `setDetail` never worked at all through the real browser code path before this task.** Every previous verification of the remesh worker (Task 16's own tests, this spec's Task 04) either used a fake/injected `RemeshRunner` (Node-testable, never touched the real `Worker`) or only checked that a production build *bundled* a separate worker chunk + `manifold.wasm` asset (Task 16's own decisions log) — nobody had actually let `setDetail` run through `createWorkerRemeshRunner` in a live browser until this task's perf verification needed it to. The symptom looked exactly like Task 04's earlier "zero-size WebGPU texture" investigation (silent, seemingly indefinite hang with zero console output) but was a completely different root cause reached the same way: **the app's own `.catch`-free `void runPerfTest(level)` call meant `setDetail`'s rejection became an unhandled promise rejection, and this Browser pane's console-reading tool does not surface those** — confirmed by instrumenting `runPerfTest`'s `await engine.setDetail(level)` with an explicit `try/catch` writing to `window.__diag2`: it actually rejected in ~15ms, every time, not hanging at all. The real error, recovered the same way (`window.__diag` logging inside `createWorkerRemeshRunner`): `WebAssembly.instantiate(): expected magic word 00 61 73 6d, found 3c 21 64 6f` — `3c 21 64 6f` is ASCII `<!do`, i.e. Vite was serving back an HTML page instead of the actual `.wasm` binary when the worker tried to fetch it. **Fix**: added `optimizeDeps: { exclude: ['manifold-3d'] }` and `worker: { format: 'es' }` to `vite.config.ts` — WASM-heavy packages commonly need excluding from Vite's dependency pre-bundling specifically because pre-bundling can mangle a package's own internal asset-URL references (exactly this symptom). Confirmed fixed in both environments: the dev-server `setDetail('med')` call now completes in ~188ms (matching a pure-Node reproduction almost exactly), and a fresh production build now correctly emits `remesh.worker-*.js` (46KB) + `manifold.wasm` (541KB) as separate assets, which it silently failed to do before (the "worked" build check in Task 16/04 only exercised `createWorkerRemeshRunner` from a *temporary* top-level `main.tsx` import, never through the real `SculptEngine` constructor call site added by Task 04 — this specific reachability path had never actually been build-tested until now).
- Task 09 — **second, separate real finding, not fixed**: `core/remesh/remesh.ts`'s edge-length estimate (Task 16, `estimateEdgeLengthForTarget`) overshoots its target triangle count more than expected in practice, and the overshoot appears to compound across successive remeshes of an already-remeshed/deformed mesh: default sphere (2,208 tri) → target 80,000 (Med) produced 96,096 (1.2× — reasonable); that 96,096-tri mesh → target 500,000 (Max) produced 860,372 (1.72×); but a **fresh** default sphere → target 500,000 (Max) directly produced 550,656 (1.1×, much closer). The spec's own wording ("toward the level's target," not an exact hit) already anticipates this being approximate, but the compounding-overshoot pattern for repeated remeshes and the resulting effect on the Q-01/BR-03 "Max clamp" (500,000, explicitly "not user-overridable") deserves a closer look — right now the actual output can materially exceed the clamp. Out of scope to fix here (it's `core/remesh/remesh.ts`'s own algorithm, Task 16's territory, not this viewport-rendering task's); flagged for a follow-up.
- **Task 09 — the ≥30fps@Max acceptance criterion does not currently pass.** Measured via the real `onFrameStats` mechanism during a scripted continuous Draw stroke (90 stamps, one per `requestAnimationFrame`, so the real render loop runs alongside exactly as a real drag would): Med detail (96,096 triangles) sustained **60.9fps** — criterion met. Max detail, tested fresh-from-default (550,656 triangles, close to the intended 500,000), sustained only **~25fps** (readings: 25.5, 24.6, 25.0) — below the ≥30fps target. This is the real, honest result of the first true end-to-end (CPU stamp + partial buffer upload + actual WebGPU render) measurement this project has ever taken at Max detail; the engine spec's own Q-01 benchmark only measured the CPU-side cost in isolation and found ≥30fps achievable there — the render cost (and/or this test harness's own per-stamp raycasting overhead, itself an added cost a real pointer-driven drag wouldn't incur, since the harness re-picks every stamp rather than reusing a single sustained raycast) evidently pushes the *total* frame budget over 33ms at this triangle count. Not investigated further (would need real GPU/CPU profiling to attribute the cost precisely, a substantial undertaking of its own) — reported honestly rather than glossed over, matching this project's established practice of accepting a PARTIAL acceptance result over a false PASS (see `sculpt-engine-core`'s own 19/23 PARTIAL outcome).
- Task 09: verified — `attachEngine` on a second engine (a differently-shaped block, attached over an already-running sphere session) correctly rendered the new mesh with no visible corruption, and a stroke run afterward on the *first* (now-detached) engine produced no error and no effect on the viewport, confirming the old `onChange` subscription was genuinely unsubscribed rather than merely overwritten-but-still-firing. Five repeated dispose→construct→init→attachEngine cycles against the same container (`Stress dispose (5x)`) all succeeded with zero accumulating console errors/warnings, and the final (5th) instance remained fully interactive (orbit still worked). React's own commit count (`useEffect` with no dependency array, incrementing a ref) stayed flat across a full scripted Draw stroke and both perf tests — no extra commits were triggered by anything the engine/viewport do internally, confirming the render loop and engine mutations are genuinely imperative and outside React, per the constitution invariant.
- **This closes all 9 tasks of the viewport-rendering plan.** Two real, unresolved findings are carried forward rather than hidden: (1) `remesh()`'s target-overshoot behavior, especially compounding on repeated remeshes (`core/remesh/remesh.ts`, sculpt-engine-core Task 16 territory), and (2) the ≥30fps@Max performance gap (~25fps measured) — both worth a dedicated follow-up investigation before this pair of specs could be called fully accepted.
