# Execution Plan: viewport-rendering

Started: 2026-07-23
Status: IN PROGRESS

Spec: docs/specs/viewport-rendering.md
Tasks: docs/tasks/viewport-rendering-tasks.md

## Progress
- [x] 01 — Install three + renderer init, capability detection, dev harness
- [x] 02 — Pure math helpers (spherical-camera, grab-projection, dirty-range)
- [x] 03 — Scene: lights, clay material, static default camera, resize
- [x] 04 — Mesh-sync: SculptMesh → geometry, partial upload, topology rebuild
- [ ] 05 — Camera controller: orbit / pan / zoom + frameModel
- [ ] 06 — Picking: raycast → SurfaceHit (point, normal, grab worldDelta)
- [ ] 07 — Pointer router: classifyMode + camera/sculpt event routing
- [ ] 08 — Brush cursor ring + mirror-plane indicator + setBrushDisplay
- [ ] 09 — Viewport facade: attachEngine, lifecycle/dispose, onFrameStats, perf verification

## Decisions & Notes
- Task 01: `three@0.185.1` ships **no bundled types** (`types`/`typesVersions` both absent from its package.json) — installed `@types/three` (matched exact version) and `@webgpu/types` (for `navigator.gpu` capability typing) separately. `three/webgpu` resolves correctly under this project's `moduleResolution: "bundler"`; verified with a throwaway probe file — testing module resolution via a direct `tsc <file>` CLI arg is misleading (it ignores `tsconfig.json` entirely and defaults to classic resolution), the probe had to live under `src/` to pick up the real project config.
- Task 01: `renderer.backend` is typed as the abstract base `Backend`, not the concrete `WebGPUBackend`/`WebGLBackend` — `isWebGPUBackend` only exists on the subclass, so backend detection uses an `in` check (`'isWebGPUBackend' in renderer.backend`) as both the runtime check and the type guard, rather than an unsafe cast.
- Task 01: `.viewport`'s CSS was `display:flex; align-items:center` (from Task 01 of sculpt-engine-core, meant only for centering placeholder text) — that container mode doesn't reliably stretch a percentage-sized canvas child. Switched to `position:relative` on the container with the canvas `position:absolute; inset:0`, which is robust regardless of the container's own layout mode. Verified visually: canvas fills edge-to-edge at both desktop and tablet viewport presets.
- Task 01: verified against a **real WebGPU backend** in this environment (`backend: webgpu` in the running app) — the WebGL2 fallback path and the "neither backend available" failure path are implemented and code-reviewed but not exercised against an actual non-WebGPU browser in this session (nothing in this environment lacks WebGPU to test against).
- Task 01: live window-resize handling (a resize listener updating `renderer.setSize` after mount) is explicitly Task 03's scope, not this one — Task 01's `resize()` call only runs once at `init()` to size correctly for whatever the container's dimensions are at mount time. Verified only that initial-mount sizing is correct across viewport presets, not live resize-while-mounted.
- First spec with interactive browser verification — most tasks are browser-verified via `npm run dev` (launch config `zmesh-dev`, port 5173), not just Vitest. Only Task 02 (pure math) and Task 07's `classifyMode` are Node-tested.
- A throwaway dev harness in `src/App.tsx` (news up an engine + viewport, mounts it) is introduced in Task 01 and fleshed out per task. The UI spec replaces it — its eventual deletion is expected, not a regression.
- Milestone shape: real sphere on screen by Task 04; full pointer→engine→mesh→screen sculpt loop closed by Task 07; end-to-end ≥60fps perf criterion (the one left PARTIAL by the engine's headless benchmark) validated in Task 09.
- Spec Open Questions planned toward their leaning answers: in-house camera math (Task 05), oriented-ring cursor (Task 08), brush radius via `BrushDisplayConfig` not an engine getter (Task 08). Revisit at implementation if any proves wrong.
- Task 07 (pointer router) is the fiddly one (trackpad gesture detection: pinch arrives as wheel+ctrlKey, etc.); kept as one task but a candidate to split into mouse vs trackpad if it gets unwieldy.
- Task 02: added `math/vec3.ts` (add/subtract/scale/dot/cross/length/normalize over a `readonly [number,number,number]` tuple), not listed in the spec's own Architecture table — all three named modules need the same handful of vector operations, so a small shared helper avoided hand-rolling cross/dot/scale three times. Framework-free like the rest of `math/` (no `THREE.Vector3`), so it stays Node-testable.
- Task 02: `framingDistance` treats the bounds diagonal as a worst-case bounding-sphere diameter (`d = diagonal / (2*tan(fovY/2))`) and applies a small feel-tuned margin (1.15×) above the tight fit so the framed model doesn't touch the exact view edge — margin only increases the returned distance, so it can't violate the acceptance criterion's "projected extent ≤ view height" (it only ever loosens that inequality further).
- Task 02: `projectScreenDeltaToWorld`'s depth is measured **along `cameraForward`** (a projection of `grabPoint - cameraPosition` onto the forward axis), not the straight-line distance to `grabPoint` — the correct quantity for a perspective FOV, and consistent with `framingDistance`'s own distance-along-view-direction convention. Screen Y is negated when mapping to the world "up" axis, since screen-space Y conventionally grows downward.
- Task 02: one test (`yields zero delta for zero screen motion`) initially failed on a `-0` vs `0` mismatch from `toEqual` on the returned tuple — `-screenDy` with `screenDy=0` produces `-0` in the up-component multiply, which is numerically identical to `0` (`-0 === 0`) but distinguished by `toEqual`'s `Object.is` semantics. Not a real bug; fixed by asserting each component with `toBeCloseTo` instead of a whole-tuple `toEqual`.
- Task 03: the static default camera's distance is derived from `framingDistance` (Task 02's math) applied to the placeholder sphere's own AABB-diagonal — not a hand-picked constant — so Task 05's `frameModel()` will reuse the exact same formula against the real mesh's bounds later, rather than the static setup and the real framing logic drifting to different tuning.
- Task 03 — **real bug caught interactively, not by Vitest**: on load, the browser console showed a cascade of `THREE.WebGPURenderer` uncaptured `GPUValidationError`s (zero-size depth-buffer/swapchain textures). Root cause: React 19 StrictMode's dev-mode mount → cleanup → mount double-invoke calls `viewport.dispose()` on the first `Viewport` instance while its async `init()` is still awaiting `initRenderer()`; `init()` had no guard against this, so it went on to build a renderer/scene/resize-observer/render-loop against a canvas already removed from the DOM (zero size) once the awaited promise resolved. Fixed by checking `this.disposed` immediately after the `await` and disposing whatever `initRenderer` produced instead of wiring it in, returning `{ ok:false, reason: 'viewport was disposed before init() completed' }`. Verified by reloading in a **fresh** browser tab (a reused tab's console buffer had stale pre-fix errors still visible, which briefly looked like the fix hadn't worked) — zero console errors after the fix.
- Task 03: resize is handled via a `ResizeObserver` on the container (not a `window.resize` listener), so container-driven layout changes (not just the browser window) are caught; each resize call also refreshes `setPixelRatio` in case DPR changed (e.g. dragging the window across displays), alongside `setSize` and `camera.aspect`/`updateProjectionMatrix` — verified visually at both mobile (375×812) and desktop presets: the sphere stays a circle, never an oval, at either aspect ratio.
- Task 04: `mesh-sync.ts` wraps the engine's `positions`/`normals`/`indices` typed arrays **directly** as `THREE.BufferAttribute`s (not copies) — FR-5's requirement, and what makes a partial sync cheap: the CPU-side geometry data is already correct the instant a stamp mutates it in place, so `sync()` only ever needs to tell the GPU which byte range to re-upload, never touch the JS-side values itself.
- Task 04: distinguishing "same topology, partial update" from "different topology, rebuild" is done by comparing `sculptMesh.positions !== currentMesh.positions` (object identity of the typed array itself), not by inspecting the `DirtyRegion`'s numeric range — a remesh always produces a brand-new `SculptMesh` with brand-new typed arrays even if it happened to land on the same vertex count, so identity is the one signal that's correct by construction, with no ambiguous edge case to reason about (e.g. "does a coincidentally-full-range stroke count as a rebuild?").
- Task 04: deliberately does **not** recompute `geometry.boundingSphere` on every partial sync (only on rebuild) — it's an O(vertexCount) scan, exactly the kind of per-stamp cost the engine spec's own Q-01 finding flagged as the scaling bottleneck. Accepted, flagged risk: a long session that pushes vertices well outside the last-computed bounding sphere (repeated Inflate, say) could in principle cause Task 06's raycasts to miss near the edge, since Three.js's raycaster uses the bounding sphere as an early-out. Revisit if that shows up once picking exists.
- Task 04: added a basic `Viewport.attachEngine(engine)` ahead of its official Task 09 slot (subscribes `onChange`, replaces the Task 03 placeholder with the engine's real mesh) — the task description itself said to "update the harness to construct a SculptEngine," which needs *some* way to get the resulting mesh into the scene, and building the simple version now / hardening it later (Task 09 adds unsubscribing a *previous* engine on re-attach) mirrors the established stub-then-complete pattern from the engine spec (e.g. Task 15's `setDetail` stub, completed in Task 16).
- Task 04 — **two real bugs caught interactively, not by Vitest, both producing the same symptom** (a cascade of `THREE.WebGPURenderer` zero-size-texture `GPUValidationError`s on load, self-recovering after a frame or two): (1) `dispose()` could remove the canvas from the DOM while `init()`'s `initRenderer()` call was still in flight — fixed by tracking the pending promise and deferring canvas removal until it settles. (2) The **actual** root cause, found by temporarily disabling StrictMode entirely and seeing the *same* errors with only one mount: the container's `clientWidth`/`clientHeight` genuinely read `0x0` on the first one or two `resize()` calls in this environment (both the manual call in `init()` and `ResizeObserver`'s own first callback), before settling to the real size — nothing to do with React at all. Fixed by skipping `resize()`'s work (and skipping `render()` in the loop) whenever the container reports a zero dimension, letting the later, real resize correct it. (1) is still worth keeping — it's a genuine, if rarer, race — but was not this bug's cause; confirmed by reproducing with StrictMode fully removed and the fix still being needed. Verified clean (zero console errors) across multiple fresh-tab reloads after the real fix, both with and without StrictMode.
